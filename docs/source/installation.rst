============
Installation
============


From PyPI (recommended):

.. code-block:: bash

   pip install galport

From source:

.. code-block:: bash

   git clone https://github.com/vdzozulia/galport.git
   cd galport
   pip install -e .